// import { logout } from "../../slices/userSlice";

// export const errorHandler = (api) => (next) => async (action) => {
//   if (action.error && action.error.status === "401") api.dispatch(logout());
//   return next(action);
// };
